"""

Purpose: optional helper utilities for preprocessing (e.g., selecting columns, converting dtypes).
When used: only if preprocessing becomes complex.
Right now: NOT needed because preprocessing is already inside fraud_pipeline.py.
We can skip it safely or remove it later.

"""